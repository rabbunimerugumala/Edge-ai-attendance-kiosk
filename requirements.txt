# Core dependencies for Touchless Smart Attendance Kiosk (Laptop & Development)
# NOTE: opencv-contrib-python is required for cv2.face.LBPHFaceRecognizer_create()
opencv-contrib-python>=4.8.0.76
requests>=2.31.0
numpy>=1.24.0
Flask>=3.0.0
psutil>=5.9.0
python-dotenv>=1.0.0

# ---------------------------------------------------------------------------------
# RASPBERRY PI 3B+ (1GB RAM) DEPLOYMENT NOTE:
# On Raspberry Pi OS (Bullseye / Bookworm), avoid pip compiling OpenCV from source.
# Instead, install pre-built system OpenCV with contrib modules via apt:
#   sudo apt update
#   sudo apt install -y python3-opencv python3-requests python3-numpy python3-gpiozero python3-pil
# For Raspberry Pi Camera Module 3:
#   pip install picamera2
# For I2C 0.96" SSD1306 OLED Display:
#   pip install luma.oled
# ---------------------------------------------------------------------------------
